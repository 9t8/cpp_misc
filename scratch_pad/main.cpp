#include <bits/stdc++.h>

class Datum {};

class SimpleDatum : public Datum {};

class List : public Datum {};

int main() {
  while (!std::cin.eof()) {
    std::cin >> std::ws;
    if (std::cin.peek() == '(') {
    }
  }
}
