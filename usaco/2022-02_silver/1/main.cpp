#include <bits/stdc++.h>

using ll = long long;

int main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);

  ll N;
  std::cin >> N;

  std::vector<std::vector<ll>> wlists(N, std::vector<ll>(N));
}
